# Local dev / seed / benchmark — these point at Docker-exposed ports on the host.
DB_HOST=localhost
DB_PORT=3307
DB_NAME=ad_analytics
DB_USER=polinus
DB_PASSWORD=adpass
REDIS_HOST=localhost
REDIS_PORT=6379